# 三大用户功能待办清单

> 儿童画廊 · 足迹地图 · 孩子说 · **攻略体系 v1.0（4 PR）**
> 最后更新：2026-07-30
> 注：第 6 节「攻略体系 v1.0」与文档 `走天下攻略体系-v1.0方案.md` (commit `1b64864`) 互为索引。
>
> **2026-07-30 状态更新**：攻略体系 PR1-4 已完整落地（commit `b7162f6`，10 条决策全实现），第 6 节任务全部标记 ✅。本节原列的"📅 待排期"均改为"✅"。

---

## 一、儿童画廊

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **Schema**：PlanMedia 加 childId + sourceType | 已合入 migration `add_guide_review_comment_and_plan_media_source_type` |
| ✅ | **API**：GET/POST /api/gallery + DELETE /api/gallery/[id] | CRUD 齐全 |
| ✅ | **页面**：/gallery（grid-cols-4 瀑布流 + 大图预览遮罩） | 已上线 |
| ✅ | **个人中心入口**：Header "+" 或 "我的" → 画廊 | 个人中心已上线（提交 f6374b7） |
| 📅 | **攻略正文图片自动提取**：建立 `contentHtml` 中的 `<img>` 遍历 + 自动录入画廊 | 待排期 |
| 📅 | **OSS 直传**：STS 凭证补真实 key | `.env:9-10` 空 |

---

## 二、足迹地图

| 状态 | 任务 | 说明 |
|------|------|------|
| 🔴 | **AMAP_API_KEY 申请** | 阻塞所有后续。需 lbs.amap.com 注册 + 2 个 key（JS API + Web 服务），写 `.env:16-17` |
| 🔴 | **Footprint 表 + API** | POST/GET/DELETE + stats |
| 🔴 | **/footprints 页面** | 先静态地图 fallback（高德 `restapi/v3/staticmap`），key 到位后换交互式 |
| 🔴 | **from-plan 足迹自动回填** | PlanRecord.status='completed' → 解析 timelineBlocks 中的 spotId → 自动创建 Footprint |
| 🔴 | **个人中心入口** | "去过的地方"选项卡 |
| 🔴 | **勋章联动** | 现有"首都足迹"勋章 criteria 改读 Footprint 表 |

---

## 三、孩子说

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **Schema**：ChildSaying 表（id, userId, childId, text, mood, shareScope） | 已合入 |
| ✅ | **API**：GET/POST /api/child-sayings | CRUD 齐全 |
| ✅ | **页面**：/child-sayings（时间线 + 5 种表情标签） | 已上线 |
| ✅ | **攻略发布页嵌入**：/guides/create 正文下方"孩子说"卡片 | 提交 84219a8，已上线 |
| ✅ | **后端入库联调**：POST /api/guides 解析 childSayings 数组 → 创建 ChildSaying | 提交 8800067，已上线 |
| ✅ | **ChildSaying 扩字段**：加 `source`/`sourceGuideId`/`status`/`spotId` | 提交 bc63743，已上线 |
| ✅ | **地点详情页"孩子们怎么说"板块** | 按 spotId 过滤 public 的 ChildSaying，提交 bc63743 |
| ✅ | **攻略发布页绑定 spotId**：childSayings 在创建时关联所选景点 | 提交 eaa075e，已上线 |
| ✅ | **攻略正文自动提取**：正则匹配引号句子 → 自动创建 ChildSaying | 提交 cfc6207，已上线 |
| ✅ | **录音功能**：MediaRecorder + OSS 上传（voiceOssKey 字段） | 提交 cfc6207，已上线 |
| ✅ | **学诗词→走天下双向联动**：诗词详情页可跳转到关联地点 | 提交 21de451，已上线 |

---

## 四、个人中心（3 功能公共入口）

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **/profile 页面**：儿童画廊 / 孩子说 / 足迹地图 三个 Tab 骨架 | 提交 f6374b7，已上线 |
| ✅ | **Header 入口**：右上角用户头像/昵称 → 跳转 /profile | 提交 f6374b7，已上线 |
| 📅 | **足迹地图 Tab 实装内容**：等 AMAP_API_KEY 后接真实数据 | 阻塞 |

---

## 五、实施建议路线

```
已完成的（2026-07-24 截止）：
  ✅ ChildSaying 扩字段（source/sourceGuideId/status/spotId）+ migration
  ✅ 地点详情页"孩子们怎么说"板块
  ✅ 攻略发布页绑定 spotId
  ✅ 攻略正文自动提取（孩子说）
  ✅ 录音功能
  ✅ 个人中心 + Header 入口
  ✅ 学诗词↔走天下双向联动

当前阻塞（等 AMAP_API_KEY）：
  🔴 足迹地图全功能（Footprint 表 + /footprints 页面 + from-plan 回填 + 个人中心 Tab 实装 + 勋章联动）

排期中：
  📅 儿童画廊攻略正文图片自动提取
  📅 OSS 直传（儿童画廊 + 录音的 STS 凭证补全）

---

# 六、攻略体系 v1.0（4 PR 切分 — 用户拍板 2026-07-28）

> 总览：[走天下攻略体系-v1.0方案.md](走天下攻略体系-v1.0方案.md)（commit `1b64864`，~470 行）
> 锁定的 10 条决策：D1 编辑权限 / D2 手动走 DFA / D3 PlanRecord 5 状态 / D4 rejected 可改重提 / D5 自动+手动都生成 / D6 published 可撤回 / D7 审核=合规+隐私 / D8 自动化+异常人工 / D9 详情页举报联动 / D10 接 apps/moderation 独立服务
> 3 层审核：L1 自动 DFA（dual threshold soft/hard）→ L2 apps/moderation 独立服务举报 / 处分 → L3 仅异常/举报进 admin 人工

## PR1 · 工具与状态字符串统一（纯后端，估 60 行） ✅ 已落地（commit `b7162f6`）

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **新建 `apps/travel-guide/src/lib/guide-status.ts`** | 5 状态 union (`draft / pending_review / published / rejected / archived`) + 中文 label map + 转换规则 |
| ✅ | **DFA 阈值拆 soft/hard** | 改 `apps/travel-guide/src/lib/moderation.ts`：敏感词（暴力/赌博/极端/未成年身份）+ 手机身份证 ≥3 → hard reject；色情/歧视 + 长度异常 → soft pending_review |
| ✅ | **更新所有 status 引用文件** | `apps/travel-guide/src/app/profile/guides/page.tsx:148`（`pending` → `pending_review`）+ `apps/travel-guide/src/app/api/guides/mine/route.ts:69`（filter `published` 严格化）|
| ✅ | **冒烟测试** | DFA soft/hard 各构造一行，profile 状态徽章正确显示 |

## PR2 · API 改造（估 200 行 + 删 ~50 行） ✅ 已落地（commit `b7162f6`）

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **`/api/guides` POST 跑 DFA** | 现版直接 published；改：跑 L1，soft → `pending_review`，hard → `rejected` |
| ✅ | **`/api/guides/from-plan/[id]` 阈值分类对齐** | 现版只走 `reviewGuide` 一刀；改用新 dual threshold |
| ✅ | **`/api/guides/from-plan/[id]` 加 owner auth** | 现版**无 auth 检查** ⚠️ — 用户 A 可代发 B 的 plan；必须修 |
| ✅ | **新建 `POST /api/guides/[id]/submit`** | status ∈ {`draft`, `rejected`} 时重提 → 跑 L1 DFA → `pending_review` / `rejected` |
| ✅ | **新建 `POST /api/guides/[id]/publish`** | 仅作者本人；跳过 DFA 直接 published (D1 子权限) |
| ✅ | **新建 `POST /api/guides/[id]/archive`** + `unarchive` | 仅作者；archived 不在公开 feed |
| ✅ | **新建 `POST /api/guides/[id]/report`** | 任何人；POST 到 `apps/moderation` 服务 `/api/moderation/report`；副作用把 guide 改 pending_review |
| ✅ | **新建 `GET /api/plans/[id]/ratings/summary`** | plan 详情页拉所有 ChildRating 聚合（per ChildProfile v1.5 5 维度评分：physicalState/emotionalPeak/stayDurationMinutes/willingnessToReturn/cryEpisodes）|
| ✅ | **改 `GET /api/guides/[id]`** | 额外返回 `sourcePlanTimeline[]` + `childSayings[]` + `ratingSummary` + `isOwnable` + `permissions`（author/admin/anyone 三层）|
| ✅ | **改 `GET /api/guides/mine`** | type 直接读 status 字符串（`'draft'` / `'pending_review'` / `'published'` / `'rejected'` / `'archived'` / `'saved'`）；tab 5+1 |

## PR3 · UI 大头（估 +850 行 ~3 个新页面 + 改 4 个老页面） ✅ 已落地（commit `b7162f6`）

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **`/profile/plans` 新建** | 4 tab: 草稿/已完成/已发/全部；卡片操作按 status 变（draft → 编辑+发攻略+删除） |
| ✅ | **sidebar + hub 加 plans entry** | `apps/travel-guide/src/components/profile/ProfileSidebar.tsx:19-27` 加 `{ href:'/profile/plans', label:'我的计划', icon:PlanIcon, countKey:'plans' }`；hub `apps/travel-guide/src/app/profile/page.tsx:82-87` 多传一个 plans count |
| ✅ | **改 `/profile/guides`** | 5 状态 tabs（draft/pending_review/published/rejected/archived）+ saved + 卡片 dropdown（编辑/撤回/发布/删除/归档/分享） |
| ✅ | **重写 `/plan/[id]`** | 现 67 行空 success card → ~280 行：hero + 状态徽章 + **Timeline viewer**（按 day 折叠，每个 block 显示 kind/title/kidHook）+ bottom sticky CTA bar（按 status 变："发成攻略"/"记录感受"/"补记") |
| ✅ | **新建 `/guides/[id]/edit`** | ~350 行；owner + status ∈ {draft, rejected} 才进；TipTap 编辑器 + 顶部 banner（状态+提交审核/发布/撤回按钮）+ 接受 `?planId=` 预填 |
| ✅ | **改 `/guides/create`** | 接受 `?source=planId&planId=X` 预填 title/cityId/days/timelineBlocks（from-plan 之后）|
| ✅ | **新组件 `components/guide/ChildSayingBlock.tsx`** | 显示 guide 关联的 childSayings 列表 + 录音/作者 chip |
| ✅ | **新组件 `components/guide/RatingSummary.tsx`** | 显示 v1.5 5 维度评分聚合 |
| ✅ | **新组件 `components/guide/TimelineViewer.tsx`** | 折叠 day-by-day 时间表渲染 |
| ✅ | **新组件 `components/guide/PlanIcon.tsx`** | 加进 Icons.tsx |

## PR4 · 详情页 + admin（估 +280 行） ✅ 已落地（commit `b7162f6` + `d4ce1b6` JWT 修复）

| 状态 | 任务 | 说明 |
|------|------|------|
| ✅ | **改 `/guides/[id]`** | 267 行 → ~450 行：加 author-only action bar（编辑/撤回/归档/分享）+ 渲染 sourcePlan.timeline + childSayings 区块 + rating 聚合 + author badges（TravelBadge） |
| ✅ | **详情页加举报按钮** | 右上角菜单，仅作者外可见；点击 → 弹原因 modal → `POST /api/guides/[id]/report` |
| ✅ | **改 `/admin` admin UI** | 现有"攻略待审"tab 升级：拉 `/api/moderation/review?type=guide`；admin tab 共用同 list view |
| ✅ | **apps/moderation 服务入参** | 确认 `POST /api/moderation/report` 接受 `targetType='guide'` + `targetId`；不存在就加上 |
| ✅ | **mobile wizard "发布攻略" 按钮** | `apps/mobile/src/pages/travel/plan-detail/index.vue:209` 当前是 toast stub；改 `uni.navigateTo` 到 `/pages/travel/wizard/step1-city?fromPlan=${id}` 或新加 `/plan/[id]` PC 路由 |

## 不在本 4 PR 范围（已知 backlog，已在 task 31/32/33 单独跟踪）

- 📅 PR2-A · LLM 自动算天数（multi-city heuristic 升级）
- 📅 PR2-B · 接 AMAP/12306 真实交通数据（替换 haversine）
- 📅 PR2-C · Hotel pipeline 补全（Hotel 表 0 行问题）

## 总览改动文件

- **PR1**: 2 文件（`lib/moderation.ts`, 新 `lib/guide-status.ts`）
- **PR2**: 5 文件（`/api/guides/route.ts`, `/api/guides/from-plan/[id]/route.ts`, `/api/guides/mine/route.ts`, `/api/guides/[id]/route.ts`；新 `/api/guides/[id]/{submit,publish,archive,unarchive,report}/route.ts`, `/api/plans/[id]/ratings/summary/route.ts`）
- **PR3**: 7 文件（4 修改 + 3 新建；components 4 个新增）
- **PR4**: 3 文件（`/guides/[id]/page.tsx`, `/admin/page.tsx`, mobile plan-detail; `apps/moderation/server/index.js` 加 target type）

合计：~17 文件 / +1400 行 −250 行
```